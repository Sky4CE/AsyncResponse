<Project Sdk="Microsoft.NET.Sdk">

  <Sdk Name="Aspire.AppHost.Sdk" Version="13.5.3" />

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <IsAspireHost>true</IsAspireHost>
    <!-- Aspire 13.5 warns (ASPIRE010) whenever AspireUseCliBundle is false — which is its own
         default. Left off deliberately: the bundle changes how DCP and the dashboard are acquired
         (through the Aspire CLI rather than the package references the SDK adds when it is off),
         and nothing here launches through the Aspire CLI — AppHosts run via `dotnet run` and
         Aspire.Hosting.Testing. Opting in is a harness behaviour change, not a version bump. -->
    <NoWarn>$(NoWarn);ASPIRE010</NoWarn>
    <UserSecretsId>asyncresponse-integrationtests-apphost-5ff760b0-7ffb-4cd9-8fe5-6464f1286486</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Aspire.Hosting.AppHost" />
    <PackageReference Include="Aspire.Hosting.Kafka" />
    <PackageReference Include="Aspire.Hosting.Redis" />
    <PackageReference Include="MessagePack" />
    <PackageReference Include="StackExchange.Redis" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\samples\AsyncResponse.Sample\AsyncResponse.Sample.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="servicebus-emulator-config.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <Target Name="BuildAspireProjectResourceDebugOutput" BeforeTargets="Build">
    <MSBuild
      Projects="..\..\samples\AsyncResponse.Sample\AsyncResponse.Sample.csproj"
      Targets="Build"
      Properties="Configuration=Debug;TargetFramework=net10.0" />
  </Target>

</Project>
